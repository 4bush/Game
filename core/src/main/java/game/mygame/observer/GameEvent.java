package game.mygame.observer;

public enum GameEvent {
    SCORE_CHANGED,
    LIFE_LOST,
    LIFE_GAINED,
    GAME_OVER,
    ENEMY_KILLED,
    GAME_PAUSED,
    GAME_RESUMED,
    BOSS_SPAWNED,
    BOSS_DEFEATED,
    POWERUP_COLLECTED
}
