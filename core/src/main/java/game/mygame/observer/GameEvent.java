package game.mygame.observer;

public enum GameEvent {
    SCORE_CHANGED,
    LIFE_LOST,
    GAME_OVER,
    ENEMY_KILLED,
    GAME_PAUSED,
    GAME_RESUMED,
    BOSS_SPAWNED,
    BOSS_DEFEATED,
    POWERUP_COLLECTED
}
